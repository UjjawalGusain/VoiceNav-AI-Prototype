const BASE_URL = "http://127.0.0.1:5000"

export const apis = {
    "transcribe": `${BASE_URL}/transcribe`,
    "navigate": `${BASE_URL}/api/navigation/plan`,
    "worker": `${BASE_URL}/api/navigation/worker`
}
